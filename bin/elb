#!/usr/bin/env ruby
$LOAD_PATH.unshift 'lib'
require 'sidekicks/elb'

elb = Sidekicks::ELB.new
elb.register

%w(TERM KILL INT).each do |signal|
  Signal.trap(signal) do
    elb.deregister
    exit 0
  end
end

loop do
  sleep 1
end
